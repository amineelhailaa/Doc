

#Questions Theoriques:

1-ddl sert a difinir la structure des donnes , par exemple on peut cree un tableau 
modifier le ( modify a column alter table tablename modify column ...) ou meme supprimer une base de donnes ou un tableau . 
dml sert a manupiler uniquement les donnees qui sont dans la structure pas plus .

2-un cle primiare est un cle qui defini une entree dans un tableau pour l'identifier apres ,
aussi il facilite l'access des donnees tout en assurant que chaque cle a une entree unique.


3- where est une simple clause qui permet de filtrer les donnees a travers passee la condition apres,
par exemple select * from users where age>18;



#Challenges Pratiques:

1:
challenge=> create table produits (
challenge(> id int generated by default as identity primary key,
challenge(> nom varchar(20),
challenge(> prix decimal(10,2),
challenge(> stock int);

2:
challenge=> insert into produits (nom,prix,stock) values ('stylo',2.5,100),
('gomme',3,50),
('trouse',3.5,200);


3:
update produits set prix = prix + prix*10% where id=1;
challenge=> delete from produits where id=1;

